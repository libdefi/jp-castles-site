import styles from "./Preflinks.module.scss";

export default async function Preflinks() {
  return (
    <section className={styles.preflinks}>
      <h2>都道府県別リンク</h2>

      <div className={styles.container} />
    </section>
  );
}
